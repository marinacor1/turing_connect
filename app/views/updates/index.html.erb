<div class="updates">
  <h1>Updates around the Turing Community</h1>
  <% @updates.each do |update| %>
    <% update.action.split(",").each do |action| %>
      <p><%= update.user.name %> in  <%= update.cohort %> <%= action.delete("[,],\"") %></p>
      <% end %>
  <% end %>
</div>
